*.iml
.idea
.DS_Store
node_modules
settings.production.json
app/.deploy/mup.js
app/.deploy/settings.json

# Claude Code local / session files (keep launch.json shared)
.claude/settings.local.json
.claude/scheduled_tasks.lock
# tools/ has its own package for build-time deps
tools/node_modules/
app/test.log

# Local evidence bytes produced by the community-ingestion worker. Metadata is
# stored in MongoDB; raw artifacts are intentionally kept out of source control.
.matchbook-artifacts/
services/community-ingestion/.artifacts/

# Generated documents. Derived from the Markdown in doc/, and it drifts.
output/
